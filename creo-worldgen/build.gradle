plugins {
    id 'fabric-loom' version '1.3-SNAPSHOT'
    id 'maven-publish'
}

version = project.mod_version
archivesBaseName = project.archives_base_name

loom {
    accessWidenerPath = file("src/main/resources/creo-worldgen.accesswidener")
}